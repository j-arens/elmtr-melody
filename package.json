{
  "name": "elementor-melody",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "author": "Josh Arens",
  "license": "ISC",
  "devDependencies": {
    "@types/backbone": "^1.3.42",
    "@types/jquery": "^3.3.0",
    "@types/node": "^9.4.6",
    "awesome-typescript-loader": "^3.4.1",
    "typescript": "^2.7.2",
    "webpack": "^3.11.0"
  },
  "dependencies": {
    "lodash.mergewith": "^4.6.1",
    "lodash.throttle": "^4.1.1"
  },
  "scripts": {
    "build:audioPicker": "webpack --config ./dev/webpack.config.js --env.index=audio-picker/index.ts --env.name=audioPicker --progress --colors",
    "build-watch:audioPicker": "npm run build:audioPicker -- -w",
    "build:adapter": "webpack --config ./dev/webpack.config.js --env.index=adapter/index.ts --env.name=adapter --progress --colors",
    "build-watch:adapter": "npm run build:adapter -- -w",
    "build": "npm run build:audioPicker & npm run build:adapter",
    "start": "npm run build-watch:audioPicker & npm run build-watch:adapter"
  }
}
